import { Row } from "./Popup.style";

const Popup = () => {
  return (
    <Row>
      <span>Cancel</span>
      <span className="current-transaction">Expense</span>
    </Row>
  );
};

export default Popup;
